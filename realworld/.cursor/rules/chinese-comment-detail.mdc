---
description: 强制中文详细注释规范（Go Kratos）
alwaysApply: true
globs:
  - "**/*.go"
  - "**/*.proto"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.py"
---

你必须严格遵守以下规则：

1. 注释语言
- 所有新增注释必须是中文。
- 除专有名词外，禁止英文整句注释。

2. 注释详细度
- 对新增或修改的函数、方法、结构体、复杂逻辑块，必须写详细中文注释。
- 每条关键注释至少说明：
  - 目的（为什么）
  - 关键行为（做了什么）
  - 边界或异常处理（失败时如何处理）

3. 代码修改约束
- 变更逻辑时同步更新注释，避免过期注释。
- 不得用无意义注释凑数量，注释必须有信息增量。

4. 分层要求（Go Kratos）
- service 层：说明接口语义、鉴权要求、错误返回约定。
- biz 层：说明业务规则、状态流转、约束条件。
- data 层：说明外部依赖、失败场景、重试与超时策略。

5. 输出要求
- 在最终说明中，明确列出本次补充了哪些中文注释（按文件路径列出）。
